import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.grilogod.boardgamescore',
  appName: 'Board Game Score',
  webDir: 'out'
};

export default config;
